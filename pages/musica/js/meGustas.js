
/*const activarBtnMeGusta = () => {
    debugger
    const btnMeGusta = document.querySelectorAll("button.tarjetaMusica__boton")
    btnMeGusta.forEach(btn => {
        btn.addEventListener("click", () => {
            darMeGusta(btn.id)
        })
    })
}
*/

/*
const darMeGusta = (meGusta) => {
    let resultado = canciones.find(element => element.meGusta === meGusta)
    if (resultado !== undefined) {
        carrito.push(resultado)
        guardarCarrito()
    }
}

const darMeGusta = (meGusta) => {
    let resultado = canciones.find(element => element.meGusta === meGusta)
    if (resultado !== undefined) {
        let likes = canciones.meGusta++
        return resultado 
    }
}


const guardarCarrito = () => {
    if (carrito.length > 0) {
        localStorage.setItem("carrito", JSON.stringify(carrito))
    }
}
*/